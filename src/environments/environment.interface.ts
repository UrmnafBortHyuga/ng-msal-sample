export interface Environment {
  production: boolean;
  salesBackendOrigin: string;
  estimateBackendOrigin: string;
  clientId: string;
  directoryId: string;
  redirectUri: string;
  apiEndpoint: string;
}
